anthropic>=0.113.0
claude-agent-sdk==0.2.97
fastapi
python-multipart
python-dotenv
uvicorn[standard]
# 琴房房间同步：小窝后端拿着 DUETTO_TOKEN 去连 Duetto 的 /ws，前端只连小窝。
# uvicorn[standard] 的 extra 里本来就带着它，但那是**传递依赖**——
# 我们自己 import 的东西要自己写在这儿，不然哪天 extra 变了会莫名其妙断。
websockets
# python:3.12-slim 里没有 /usr/share/zoneinfo，ZoneInfo("Asia/Shanghai") 会抛
# ZoneInfoNotFoundError。zoneinfo 找不到系统库时会自动回落到这个包。
# 没有它，用户消息前面那行时间会静默消失（_now_line 吞掉异常返回空串）。
tzdata

chromadb>=0.5.0
jieba
rank-bm25

# 琴房频谱：在本地把歌算出来（BPM/调性/能量/频谱图），不经过任何 AI 接口。
# 🔴 只在 app/piano_analysis.py 的函数里 import，不许放模块顶部——
#    顶部 import 会把常驻内存永久抬高，队列 38 正在查小窝为什么涨到 189MiB。
librosa
matplotlib

# Web Push（砖 1）。🔴 只在 app/push.py 的函数里 import，不许放模块顶部——
# 它拖着 requests + cryptography，顶部 import 会把常驻内存永久抬高，
# 跟 librosa/matplotlib 一个待遇（队列 38 在查 189MiB 那件事）。
pywebpush

# 一起看书（书房第五栏）：app/anno.py 把 /marginalia/api 流式转发给 anno 的
# Node 服务。httpx 本来就随 anthropic 进来了，但那是**传递依赖**——
# 我们自己 import 的东西要自己写在这儿，跟上面 websockets 一个道理。
httpx
