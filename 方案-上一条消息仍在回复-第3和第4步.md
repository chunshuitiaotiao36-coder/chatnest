# 方案：第 3 步和第 4 步（**没动代码，先给你看**）

施工单第 7 节要求「第 3、4 步是硬骨头，做之前把方案写给她看」。这就是那份方案。
第 1、2 步的代码已经推上 main 了（**还没部署**——你去 Coolify 点一下 Redeploy，
约四分钟）；这一份方案在你说「行」之前，一行代码都不会动。

---

## 先说结论

**第 3 步我建议做，但要按「放锁不取消」的形态做，而且要等探针的数据回来。**
**第 4 步现在做不了**——它整个建立在第 4.4 节那三个数上，那三个数得你先用两三天。

---

## 第 3 步：让后端知道你已经走了

### 要解决的是什么

你 abort 之后（点停止、关标签页、切走），后端那一轮还在跑满 180 秒，
这 180 秒里你发的每一条都会撞在 `chat_lock` 上。第 2 步把「秒拒」改成了
「等 4 秒」，所以现在**短的收尾能等到了**；但如果上一轮是真卡住的那种，
4 秒等不到，你还是会被挡将近三分钟。

### 那个必须先想清楚的坑

施工单自己写了：**取消得太积极，就变成「切一下后台回复就没了」。**
那是你会更生气的 bug。你在手机上切个 App 回来，本来想看到那条回复，
结果它被我主动掐了——这比「等三分钟」糟。

所以**不取消**。方案是把「持有锁」和「这一轮还在跑」拆开：

```
现在：  [拿锁] ── 整轮 ─────────────────────────► [放锁]
                （你走了，锁照样握着，一直到 180 秒）

改成：  [拿锁] ── 整轮 ──►（发现你走了）[放锁] ── 这一轮继续跑完 ──► 写库
                                          ↑
                                    下一条消息现在就能发
```

也就是：**你走了 → 放锁，但这一轮不取消**，让它在后台把结果写完存进库，
你回来照样能在这个会话里看到那条回复。同时你的下一条消息不再被挡。

### 具体怎么做

1. `sse()` 里起一个伴随任务，定期 `await request.is_disconnected()`。
   **必须主动查**——卡住的那一轮正好什么都不写，等写失败是等不到的
   （Starlette 只在往连接里写东西的时候才发现对端走了）。
2. 查到断开：立刻 `chat_lock.release("client_gone")`，但**不 cancel** 这一轮。
3. 这一轮自己跑完之后照常 `complete_turn(...)` 落库，只是没人在听 SSE 了。
4. `finally` 里要能分清「已经放过了」和「还没放」，不能放两次
   （放两次 `asyncio.Lock` 会直接 `RuntimeError`，那是把一个 bug 换成另一个）。

### 我担心的两件事，先摆出来

- **`chat_lock` 放早了，`subscription_env` 那条约束还成立吗？**
  成立，但**只在网页这一侧**：网页端从来不碰 `ANTHROPIC_*`，它只是"占着"。
  真正危险的是 TG——TG 那一轮如果提前放锁，网页端就可能在环境变量被 pop
  的窗口里挤进来。所以**这个机制只给网页端开，TG 那条线一个字不改**
  （TG 本来也没有"客户端断开"这回事：你发完就放下手机了）。
  这条我会在代码里写成红字注释。
- **落库那一步现在挂在 SSE 生成器里**。生成器被 close 掉之后它就不跑了。
  所以真做起来，得把「跑这一轮 + 落库」从生成器里挪到一个独立的 task，
  SSE 只是**读**那个 task 的输出。这是这一步真正的工作量所在，
  不是一个 `is_disconnected()` 就完事——我不想让你以为这是小改动。

### 为什么还要等数据

第 1、2 步装的探针里有一条叫 `turn_outcome`：这一轮是正常结束、报错、
还是 `client_gone`（生成器被 close 掉了）。**如果流水账里根本不出现
`client_gone`，就说明"客户端断开"这条信号压根传不到后端**——那第 3 步
就得换个做法，现在写的方案全白搭。先看数据，别猜。

---

## 第 4 步：为什么那一轮会卡

**这一步现在不能写。** 施工单第 4.4 节说得很清楚：拿到那三个数之前不写。
三个假设的排查代价差很多，猜错一个就是一整轮返工——导航栏那次就是这么没的。

要你帮我拿回来的三个数（都在你已经能看见的地方）：

1. **卡住的当时，Home →「回复状态」那一行写的是什么。**
   「空闲」还是「正在回复（已 N 秒 · 小窝/TG）」？N 大概多少？
   - 写「空闲」→ 根本不是 `chat_lock`，是 actor 那两道门之一，
     那就看报错文案里的那个括号（现在四句话不一样了）。
   - 写「正在回复」而且来源是 TG → 是两条线撞车，跟上下文无关。
   - 写「正在回复」来源是小窝、N 快到 180 → 就是那一轮真的卡住了，进第 4 步。
2. **那一轮在日志里停在哪个阶段**（`chat_timing` 打的 stage，我来翻）。
   停在 `actor_cold_start` 之后没有 `first_text_token`，跟停在
   `first_text_token` 之后没有 `done`，是完全不同的两个病。
3. **卡住的那轮有没有工具调用**（气泡里有没有出现过工具那一行）。
   有 MCP 调用 → 优先查 `anno_mcp_servers()` 那条链路的超时；
   一次都没有 → 优先查上下文长度那条。

拿到这三个数，我才知道该动 `claude.py` 的超时、还是 `_summary_model` 那条
压缩路径、还是把 actor 的 180 秒整轮超时改成「首字超过 X 秒没来就重连」。

---

## 所以现在轮到你

第 1、2 步推上去了，**点一下 Redeploy 就能用**。下次再犯的时候，
去 Home 看一眼那行「回复状态」，把上面那三条里你看得到的告诉我就行。

你说「行，第 3 步照这个做」，我就开工；你觉得哪儿不对，我就改方案。
在那之前它一行代码都不会动。
