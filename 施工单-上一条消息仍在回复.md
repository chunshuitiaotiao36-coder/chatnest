# 施工单：「上一条消息仍在回复」——发不出消息

写给接手的 CLI。**这是修 bug，不是加功能**，跟 telemood 那张单子性质不同：
有一条我已经查实的链路，也有一层还没测出来的未知量。第 4 节要求先装探针，
别跳过去直接改。

优先级：**最高**。她原话是「直接影响了我不想在小窝说话」。

---

## 0. 症状（她的原话，一字不改）

> 不知道是上下文长了还是怎么了，我记的也没有很长，转了半天显示不出来就直接
> 显示"上一条消息仍在回复"，调了 mcp 后也经常这样，重新生成没用，退出后台
> 再打开也没用，我连重新部署都试过了，没用。。有时候重新生成上上条回复能好，
> 再聊几条就又发癫

把这段读三遍。里面有四条判据，任何修法都要能解释它们：

| 判据 | 它排除了什么 |
|---|---|
| 转了半天**才**报错 | 不是一上来就拒绝，是先卡住 |
| 重新生成**没用** | 重试路径跟正常路径撞同一个东西 |
| 退出后台再打开**没用** | 前端状态清零救不了，问题在后端 |
| 重新生成**上上条**有时能好 | 跟这一轮的上下文有关，不是纯随机 |

---

## 1. 已经查清的链路（我读过代码，不是猜的）

### 1.1 那句话有三个出处，先分清是哪一个

```
app/main.py:750      "上一条消息仍在回复"   ← chat_lock 被占
app/registry.py:83   "上一条消息仍在回复"   ← actor.busy
app/registry.py:56   "另一段会话仍在回复"   ← 换会话时上一个还忙
app/actor.py:131     "上一条消息仍在回复"   ← actor.busy
```

**三处文案一模一样，所以现在没人分得清她撞的是哪一个。**
第 4 节的第一件事就是把它们区分开。

### 1.2 主链路：`chat_lock` 是**进程级单锁**

```
app/main.py:94        chat_lock = asyncio.Lock()        # 全进程一把
app/main.py:748-750   if chat_lock.locked(): 直接吐错并 return   ← 不排队，秒拒
app/main.py:755       await chat_lock.acquire()
app/main.py:1064-65   finally: chat_lock.release()
```

抢这把锁的有：网页聊天（main.py:755）、Telegram（telegram.py:878）。
只读地看一眼就让路的有：keepalive（keepalive.py:347）、
凌晨守护（nightguard.py:241）——这两个没问题，别去动。

### 1.3 🔴 我认为的主因：前端 abort 之后 **0 毫秒** 就重发，而后端根本不知道她走了

```js
// static/index.html:3453
if(state.busy){ stopActiveResponseNow(); await new Promise(r=>setTimeout(r,0)) }
```

`stopActiveResponseNow()`（index.html:3436）只做了 `controller.abort()`——
那是**客户端**断开 fetch。然后等 0 毫秒，立刻 POST 新的 `/api/chat`。

后端这边：`sse()` 那个异步生成器还卡在 `await` 里等模型。
**Starlette 只有在下一次往连接里写东西时才会发现对端走了**，而"卡住的那一轮"
恰恰什么都不写。所以：

- 客户端 abort **不会**让后端提前释放 `chat_lock`
- 那把锁要一直握到这一轮自己结束——最长 `RESPONSE_TIMEOUT_SECONDS = 180` 秒
  （actor.py:70）
- 这 180 秒里她**每一次**重发、重新生成，都会在 main.py:748 被秒拒

**这条完整解释了四条判据里的前三条。** 「转了半天」= 那 180 秒；
「重新生成没用」「退出后台再打开没用」= 后端压根不知道她做了这些动作；
「重新部署没用」= 部署完她再发一条，又卡住，又是 180 秒。

### 1.4 还没查清的一层：**为什么那一轮会卡住**

第四条判据（重新生成上上条有时能好）指向上下文/工具调用，不是随机。
她自己的猜测是"上下文长了"，而重新生成上上条会**截短对话**——这和她的猜测
是自洽的。另外她提到"调了 mcp 后也经常这样"，anno / ombre 的 MCP 调用走 HTTP，
是另一个可能的卡点。

**这一层没有测出来之前不许下结论。** 见第 4 节。

### 1.5 一个真实但次要的泄漏窗口

```python
# app/main.py:755-763
await chat_lock.acquire()
conv_id = None
user_message_id = None
...
act_stripper = piano.ActStripper()      # ← 这几行在 try 外面
split_stripper = piano.SplitStripper()
try:
    ...
finally:
    chat_lock.release()
```

acquire 之后、`try` 之前有大约十行。**这中间任何一行抛异常，锁就永久泄漏**，
只能重启进程。窗口很窄（都是赋值和两个构造函数），大概率不是她碰到的那个，
但顺手补上，一行缩进的事。

---

## 2. 🔴 硬约束：四件不许做的事

### 2.1 不许删掉 `chat_lock`，也不许简单地改成"每个会话一把"

看 `app/relays.py:345-356`：

> 订阅模式靠 pop 掉三个 `ANTHROPIC_*` 让 CLI fallback 到 `~/.claude` 的凭据，
> 而环境变量是**进程级**的，没法只对 TG 那条协程单独 unset。
> 安全性靠 chat_lock 保证——小窝和 TG 抢的是同一把锁，不会并发。
> **调用方必须已经持有 chat_lock。**

**这把锁在守一个进程级的全局状态。** 把它去掉或者按会话拆开，就会出现
「TG 把 `ANTHROPIC_*` pop 了，网页端的请求正好撞上来」——症状是网页端
莫名其妙用了订阅额度，或者反过来。那种 bug 比现在这个难查十倍。

要改并发模型，就得**先**把 `subscription_env` 那段从"改进程环境变量"
换成"把凭据显式传给这一轮"，让它不再依赖全局。那是一个独立的、更大的改动。
**本单不要求你做这件事**；本单要求你在不破坏它的前提下解决排队问题。

### 2.2 不许把 `RESPONSE_TIMEOUT_SECONDS` 从 180 调小来"解决"问题

那不是修，是把「卡 3 分钟」换成「卡 1 分钟」。而且 actor.py:68 那段注释写了
180 的来历（实测长回复带思考和工具调用可能到几十秒），调小会误伤真实的长回复。

### 2.3 不许让 `_allowed()`、白名单、鉴权相关的任何东西变松

跟这个 bug 无关，但改 main.py 的时候容易顺手碰到。别碰。

### 2.4 不许静默降级

AGENTS.md 第 3 条。锁被谁占着、占了多久，必须能在日志**和她能看见的地方**
查到。她不看日志。

---

## 3. 她现在手上有的唯一诊断手段

```
GET /api/keepalive/status   →  {"chat_locked": true/false, ...}
```

`app/main.py:1617`。这个字段已经在了，但：

- 只有 true/false，**不知道是谁占的、占了多久**
- 她不知道有这个东西

第 4 节要把它变得有用。

---

## 4. 先测后修（这一节不许跳）

导航栏那次的教训写在 `full-stack/AGENTS.md` 第 2 条：云端浏览器三次给出与真机
相反的结论。这个 bug 同理——**它只在她的真机、她的真实对话上复现**，你在
本地用短对话是测不出来的。所以先让它可观测，再动手。

### 4.1 把四个出处的文案区分开

`main.py:750` / `registry.py:56,83` / `actor.py:131` 现在文案重复。
各自带上不同的、她看得懂的后缀，例如：

- chat_lock 被占 → 「上一条还在回，等我 N 秒」（N 是这把锁已经被占的秒数）
- actor.busy → 「上一条还在回（会话忙）」
- 换会话 → 「另一段对话还没回完」

**为什么这条排第一**：不做这个，后面所有的修都是在猜她撞的是哪一道门。

### 4.2 给 `chat_lock` 加一层薄封装，记录持有者和时长

不要直接用裸 `asyncio.Lock`。包一层，记住：

- 谁拿的（`web` / `tg` / `nightguard`，以及 request_id）
- 什么时候拿的（monotonic）
- 上一次是谁、持有了多久、怎么结束的（正常 / 超时 / 异常 / 客户端断开）

然后把这些塞进 `/api/keepalive/status`，并且在**持有超过 30 秒**时打一条
WARNING 到 uvicorn 日志。

### 4.3 让她能在手机上看见

`/api/keepalive/status` 她够不着。在 Home →「用量记录」那一层，或者随便一个
她点得到的地方，加一行只读状态：**「当前：空闲 / 正在回复（已 N 秒，来源 X）」**。

**这是这一单里她唯一能自己判断"是不是又犯了"的手段。** 别省。

### 4.4 装完探针，让她用两三天，把这三个数拿回来

1. 卡住的时候 `chat_locked` 是不是 true，是谁占的，占了多久
2. 那一轮在日志里停在哪个阶段（`chat_timing` 已经在打 stage，见 main.py:716）
3. 卡住的那轮有没有 MCP 工具调用

**拿到这三个数之前，不要写第 5 节的第 3 步。**

---

## 5. 修复顺序

### 第 1 步：把泄漏窗口关掉（可以立刻做，零风险）

把 `main.py:755` 的 `await chat_lock.acquire()` 之后那十行挪进 `try`，
或者直接改成 `async with chat_lock:`。保证任何异常路径都会释放。

### 第 2 步：秒拒 → 有限等待（这是她感受最直接的一步）

现在是 `if chat_lock.locked(): 报错 return`。改成：

- 先尝试**带超时地等**这把锁（建议 3–5 秒，具体值你测完再定）
- 等到了就正常走
- 等不到才报错，而且错误里要带上「已经被占了 N 秒」

理由：她的绝大多数重试发生在 abort 之后的一瞬间（index.html:3453 那个 0ms）。
真实场景里上一轮往往正在收尾，等几秒就能拿到。

**同时改前端**：`stopActiveResponseNow()` 之后那个 `setTimeout(r,0)` 太急。
但**不要**简单地把 0 改成 3000——那会让她每次重发都干等 3 秒。
正确的做法是让后端的等待来承担这件事（前端照发，后端排队），前端只负责
把「正在等上一条收尾」这个状态显示出来，别让她以为卡死了。

### 第 3 步：让后端知道她已经走了

客户端 abort 之后后端还在跑满 180 秒，这是浪费也是主因之一。
`Request.is_disconnected()` 可以查，但**注意**：卡住的那一轮正好不写东西，
所以要主动去查，不能等写失败。

一个可行方向：在 `sse()` 里起一个伴随任务定期 `await request.is_disconnected()`，
发现断开就取消这一轮并释放锁。**但这里有个坑必须想清楚再动手**：
她退到后台可能只是切了个 App，回来还想看到那条回复。取消得太积极，
就变成「切一下后台回复就没了」——那是另一个她会更生气的 bug。

所以这一步的正确形态多半是：**断开时不取消，但把锁放了**，让这一轮
在后台把结果写完存进库（她回来能看到），同时不再挡着下一条消息。
这需要把「持有锁」和「这一轮还在跑」解耦。**做之前先把方案写下来给她看。**

### 第 4 步：查"为什么会卡"（等第 4.4 节的数据回来再做）

假设按可能性排序：

1. **MCP 工具调用没有超时**。anno / ombre 走 HTTP，对面不回就一直等。
   查 `app/claude.py` 里 `anno_mcp_servers()` 那条链路有没有超时。
2. **上下文长度**。她说重新生成上上条能好。如果数据支持，就要看
   长上下文下 CLI 子进程的行为，以及 `_summary_model` 那条压缩路径。
3. **CLI 子进程本身不发结束标记**。actor.py:68 的注释说这在订阅线路上实测
   出现过（没有 first_text_token、没有 ResultMessage、也没有异常）。
   如果是这个，180 秒超时就是唯一的兜底，得往前挪——比如
   「首字超过 X 秒没来就重连」而不是「整轮超过 180 秒」。

---

## 6. 验收

- [ ] 四个出处的报错文案各不相同，她一眼能看出撞的是哪一道
- [ ] `/api/keepalive/status` 能说出锁被谁占了、占了多久
- [ ] 她在手机上点得到那个状态
- [ ] 持有超过 30 秒有 WARNING 日志
- [ ] `chat_lock` 在任何异常路径下都会释放（写个测试，故意在 acquire 之后
      抛异常，断言下一次请求不被拒）
- [ ] 上一轮正在收尾时立刻重发，能等到，而不是秒拒
- [ ] 🔴 TG 和网页交替发消息，`subscription_env` 那段的环境变量互不串台
      （relays.py:345 那条约束没被破坏）
- [ ] 现有 5 个测试文件仍然全过：
      `test_piano_act.py` `test_piano_impression.py` `test_piano_queue.js`
      `test_piano_sheets.js`（`test_piano_ws.py` 在本环境因 SDK 的类型问题
      起不来，跟本单无关，别去修它）

---

## 7. 交给她之前要说清楚的一句话

第 2 步做完就该让她先用起来——那是她感受最直接的一步。
第 3、4 步是硬骨头，**做之前把方案写给她看**，尤其是第 3 步那个
「切后台会不会丢回复」的取舍。她为一个导航栏耗过一整个下午到凌晨四点，
不要再给她一个"修好了但换了个方式坏"的东西。
