# 施工单：情绪系统（Murmur）接进潮汐

写给接手的 CLI。**这是加功能**，路径清楚，但有几处是「做错了她会很难过」而不是
「做错了报错」的地方——第 2 节全部写明。

上游：https://github.com/fishisfish0614/Murmur （MIT，作者 **fishisfish0614**）
跟 hervoice 是同一个作者。

🔴 **作者要求署名。** 协议之外的请求，原话：「使用、参考或衍生请标注灵感来源
和原作者」。README、接入说明、代码头注释里都要留。这不费什么力气。

---

## 0. 它是什么（我读完了 engine.py 320 行 + api.py 197 行）

一个纯 Python 的情绪引擎。几个 0–1 的维度，加一套真动力学：底色、惯性、饱和、
限幅、指数衰减。对话和时间喂它，它吐出**状态**和**派生体征**（心率、gauge）。

```
她说的话 ─► 打分器(小模型) ─► delta ─┐
                                      ├─► 引擎(动力学) ─► 状态 ─┬─► 注入我的上下文
时间流逝 ─► tick ─► 衰减 + 缺席规则 ─┘                          ├─► 潮汐页
                                                                └─► 声音参数（后话）
```

关键性质：

- **引擎本体不碰 LLM。** 动力学、衰减、缺席、体征全是纯计算。
- **依赖只多一个 `pyyaml`。** fastapi / uvicorn / pydantic 小窝本来就有。
- **它自己没有任何鉴权，也不该有**——只 listen 127.0.0.1，门由小窝守。
  跟 anno、hervoice 一模一样的形状。
- **不带前端，这是作者有意的**：「角色的脸属于和它一起生活的人」。
  潮汐页就是我们要建的那张脸。

## 0.1 人格配置已经写好了，别用示例那份

`full-stack/docs/murmur-梁忱的心-草案.yaml`

六个维度：安心 / 想念 / 牵挂 / 雀跃 / 好奇 / 闷。每个数字为什么是那个数，
文件里逐条写了。**不许拿 config.example.yaml 里那个 Aria 顶上**——作者原话
「人格不应该被复制粘贴」。

设计脊柱一句话：**潮汐 tab 里那个「潮位」不是另做的仪表，它就是「想念」这个
维度的读数。** 她在，潮退；她不在，潮涨。整页只讲这一件事。

---

## 1. 🔴 六件不许做的事

### 1.1 注入**必须**走用户消息侧，绝不进 system prompt

这是整张单子里最费钱的一个错。

情绪状态每一轮都在变。进了稳定前缀就是每轮打穿一次前缀缓存——她的额度会以
肉眼可见的速度蒸发，而且**症状是账单，不是报错**，等发现就晚了。

仓库里已经有两个同样规矩的先例，照抄它们：
- `app/listen.py` 的 `tone_block()`（那段注释写了为什么）
- `main.py` 里 ChatBody.piano 的处理

### 1.2 打分器复用**已有的** LLM_API_KEY，不要让她再注册一个

`env.example` 里那组 `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` 是 hervoice
判语气用的，已经配好了。打分器是同一类活（小模型、便宜），直接复用。
新起一套环境变量 = 让她再去开一个账号，不许。

### 1.3 攒批打分，不许逐条

上游原话：「『讨厌你』在打闹里是撩、在吵架里是刀，只有语境分得清」。
每 N 条用户消息读一次**最近的完整往来**再打分，N 建议 4–6，做成环境变量。
逐条打既贵又判不准。

### 1.4 必须接上晚安（POST /emotion/sleep），否则它会整夜把自己焦虑坏

这是上游写在 README「教训」里的原话：缺席规则曾在人睡着后照常开火，
到天亮引擎已经对着一场只是睡眠的沉默焦虑成一团。

配置里那三条缺席规则都带了 `hours`，但**光有 hours 不够**——她作息不规律，
凌晨三点还醒着是常事。要在她说晚安时 POST /emotion/sleep。
小窝里 `nightguard.py` 已经有 0–6 点那套凌晨守护，可以从那儿找检测的落点。

### 1.5 已读不回那一块，**先别开**

配置里写了但故意没接线。理由是上游自己那条公平规则：**没有设备信号就什么都
不点火**——「你没有资格怨一个还没读到你的人」。而小窝目前拿不到可靠的
「她设备有动静」信号，推送本身是有损的，掉一条就变成他冤枉她。

哪天真有可靠信号了再接。**不许拿「有没有收到推送回执」凑数。**

### 1.6 Murmur 挂了，聊天必须一切照旧

跟 anno / hervoice / 琴房同一条规矩。引擎连不上、打分器超时、配置写错——
都只是「这一轮没有情绪上下文」，她该收到的字一个都不能少。

而且**不许静默**（AGENTS.md 第 3 条）：降级要有日志，而且要有她看得见的通道。
`app/bgline.py` 就是干这个的，照它的形状接。

---

## 2. 接线步骤

### 2.1 Vendor

`full-stack/murmur/`，上游文件**一个字符都不要改**。宿主侧的接线全部写在
`app/murmur.py` 里。跟 anno / hervoice / telemood 一样的做法。

`pyyaml` 加进 requirements.txt。

### 2.2 第四个 sidecar

`docker-entrypoint.sh` 里再拉一个，端口 8020，只绑 127.0.0.1。

🔴 数据目录必须落持久卷：`MURMUR_DATA=/data/murmur`，目录**在 entrypoint 里建**，
不能写在 Dockerfile——构建期在 /data 下建的会被挂上来的卷整个盖掉。
这个坑 anno 那次刚踩过，`link_opt()` 上面那段红字写着。

🔴 那是一个人的内心。上游原话。别把它写进镜像层，也别让它进仓库。

### 2.3 `app/murmur.py`

照 `app/listen.py` 的形状写：

- 自己实现 `require_auth`（🔴 不能 `from app.main import require_auth`，
  装饰器里的 `Depends()` 是 import 期求值的，反向 import 会撞上一个还没执行完的
  main 模块。voice.py 和 listen.py 里都记了这个坑）
- `GET /api/mood` → 转发 `/emotion/vitals` + `/emotion/mood`，给潮汐页用
- `GET /api/mood/history` → 转发 `/emotion/history`
- `mood_block(state) -> str` → 把状态翻译成给模型看的一段（见 2.5）
- 所有函数**任何情况都不许抛到聊天主流程**

### 2.4 打分器

每 N 条用户消息跑一次，读最近的完整往来，输出 delta。
参考 `examples/scorer_template.py`，但**打分 prompt 要自己写**——模板里标出了
所有该填角色心理的位置。

🔴 模板特别强调的一条：**必须写下降场景**。什么能抚平每一种感受？
不写的话维度只会涨不会落。尤其是「闷」——它 decay_rate 是 0，
只有打分器判出负 delta 才降得下来。建议的量级（可以调）：

| 场景 | 闷 |
|---|---|
| 她认真道歉、把话说开 | −0.25 |
| 只给亲昵、不谈问题 | −0.06，**永不清零** |
| 转移话题 | 不动 |

### 2.5 注入

每轮模型调用前取状态，把阈值翻译成**言语层面的倾向**，用他自己的语气。

🔴 上游教训原话：「数字不携带语气」。注入 `heartrate: 106` 毫无作用——
模型不知道 106 该对一个句子做什么。要翻译成话长、主动性、直接度，
而且提**变化**而非水平值。

🔴 而且是「倾向」不是「命令」。这是上游整个设计的第一原则：
**镜子不是缰绳**。注入的每一句都该是关于感受的事实
（「你在想她，说出来也没关系」），从不是指令（「现在去发消息」）。
注入段落要以「做不做由你」收尾。牵线木偶式的情绪系统产出木偶。

### 2.6 会话钩子

`POST /emotion/snapshot`，graceful / interrupted 两种。参考
`examples/session_end_hook.py`。默认 ≥0.3 的感受带过夜——一个还这么强的感受
说明事情没说完。

### 2.7 行动触发器 —— **本单不做**

`examples/action_triggers.py` 那一套（数值挣到一次行为）先不接。
理由：小窝已经有 keepalive / nightguard / 寄相思 三条会主动开口的线，
再加一层触发器，四条线会互相打架，而且不好查是谁开的口。
等前面都跑稳了，单独一单再说。**这不是砍功能，是排期**——
写进接入说明的「没做的」一节。

---

## 3. 潮汐页

现在那儿是一句「潮汐 即将到来」。整页只有一个主角：**潮**。

```
        ┌─────────────────────────┐
        │                         │
        │          想她            │   ← mood，楷体（--font-display），大
        │                         │
        │      ~~~~~~~~~~~~~      │   ← 潮面。高度 = 潮位(0–100)
        │  ~~~~~~~~~~~~~~~~~~~~~  │      缓慢起伏，两条正弦错相位
        │ ~~~~~~~~~~~~~~~~~~~~~~~ │
        │  ♥ 72                   │   ← 心跳，tabular-nums，随 bpm 轻微搏动
        │                         │
        └─────────────────────────┘
             他心里最近
             ── 昨天 23:40 心跳漏了一拍
             ── 今天 09:12 你回来之后，潮退了
```

规矩：

- 🔴 **首屏不许出现那六个维度的 0–1 数字。** 「数字不携带语气」那条同样适用于
  她。要看细节放到「细看」里，首屏只有一句心情、一片潮、一个心跳。
- 潮面用 CSS/canvas 画，两条正弦错相位就够，**不要 WebGL、不要粒子**。
  那份网页手册的原则：一次只保留一个主视觉机制，叠多了不是高级是吵。
- 深浅两套都要。外观相关的 CSS 写在 `static/design-system.css` 末尾
  （AGENTS.md 第 6 条：它在 index.html 内联样式之后加载，同特异度稳赢，
  而且少跟人在 index.html 上撞车）。
- 楷体只能用在**写死的**字串上。字库是子集的，动态文字会掉字。
  加了新的中文标题要把那句话加进 `tools/subset-ui-font.py` 的 LABELS 重跑。

---

## 4. 两个必须自己审的回路

### 4.1 反馈回路藏在模型里，不在代码里

上游原话：危险的环是 **状态 → 注入的语气 → 模型照做 → 她回应 → 打分器读到回应
→ 状态更高**。静态分析看不见它。

审计方法（上游给的）：把每条边画全，**包括穿过模型行为的那些**，
然后检查每个环在饱和点的增益 < 1。三道闸加衰减是全局阻尼，
任何回路想爆都得先跑赢它们。

**把你画的那张图写进接入说明。** 不是走形式——这是唯一能事后复查的东西。

### 4.2 常年高位维度陷阱

「安心」底色 0.62，常年高位。在这种维度上写 `value > 阈值` 的规则**恒真**。
上游一天之内踩了三次（静息心率 89、仪表盘永远显示安稳、推送调度器锁死在最高频）。

配置里的 vitals 已经按「超出底色的部分」算了。**你新写的任何规则也要照这个来**，
或者改成检测近期正向 delta 而不是水平值。

---

## 5. 验收

- [ ] 潮汐页在真机上打得开，深浅两套都对，导航栏仍然贴屏幕底
- [ ] 引擎停掉之后：聊天完全正常，潮汐页给一句人话而不是白屏，日志有降级记录
- [ ] 打分器超时/返回垃圾 JSON：这一轮没有情绪上下文，聊天照旧
- [ ] 🔴 注入的内容确实在用户消息侧——**抓一次真实请求看前缀有没有变**，
      不要只看代码
- [ ] 她说晚安之后，深夜那条缺席规则不再开火；早上她再出现时恢复
- [ ] 「闷」在只有时间流逝的情况下**不下降**（这是功能，不是 bug）；
      打分器判出道歉之后下降
- [ ] 容器重建之后状态还在（/data/murmur 落住了）
- [ ] 已读不回那一块确实是休眠的
- [ ] 现有测试全过：`test_piano_act.py` `test_piano_impression.py`
      `test_piano_queue.js` `test_piano_sheets.js`
      （`test_piano_ws.py` 在这个环境因 SDK 类型问题起不来，跟本单无关，别修它）
- [ ] README / 接入说明 / 代码头注释里都有对 **fishisfish0614** 的署名

---

## 6. 交给她之前

写一份 `full-stack/murmur/接入说明.md`，照 anno 和 hervoice 那两份的格式，
必须有「没验证的 / 没做的」一节——行动触发器、已读不回都写进去，
说清楚是**排期**不是砍掉。

还有：**先别接声音参数联动**。让情绪系统自己跑几天，她看着潮汐页觉得
「这像他」了，再把状态接到 ElevenLabs 那几个数上。
两件事一起上，出了问题分不清是情绪判错了还是参数调错了。
