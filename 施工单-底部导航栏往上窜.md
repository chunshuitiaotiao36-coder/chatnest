# 施工单：底部导航栏「点一下窜一截」

写给接手的 CLI。**先读完第 1、2 节再动手。这个 bug 已经修了 8 次全部失败，
失败的原因不是修得不对，是每次都跳过了「先确认是哪种机制」这一步。**

---

## 0. 现象（用户原话 + 截图证据）

底部导航栏 `#tabBar` 不在屏幕底部。**反复点导航栏切 tab，每点一次就往上窜一截，
累积、不回弹**，最后停在屏幕中间偏上，下面一大片死区。

三张真机截图（iPhone，主屏 PWA，standalone，`viewport-fit=cover`）：

| 时间 | 所在页 | 导航栏底边位置 | 屏幕下方死区 |
|---|---|---|---|
| 03:07 | 书房（浅色） | 屏幕高 ~56% | ~365pt |
| 03:33 | Home（深色） | 屏幕高 ~76% | ~208pt |
| 04:15 | 琴房（浅色） | 屏幕高 ~62% | ~325pt |

三张图共同点，**每一条都要拿来做判据**：

1. 导航栏本身**长相完全正确**——五个 tab 等宽、铺满整屏、图标标签齐整、
   有顶边线和玻璃底。
2. 页面内容**正好在导航栏那一行被切断**。04:15 那张琴房只到进度条，
   下面的播放控制按钮整排没了。
3. 导航栏下方那片死区是**纯底色**，不是页面内容，也不是键盘。
4. 键盘**没有出现在画面里**。

---

## 1. 已经推定出来的结论（不要重新推）

### 1.1 `position:fixed` 是生效的，导航栏没有掉进普通流

这是本单最重要的一条，它一次性排掉了两个跑了很久的方向。

`#tabBar` 的定位和长相**写在同一条规则里**（`index.html:173`）：

```css
#tabBar{position:fixed;left:0;right:0;bottom:0;z-index:81;display:flex;
        align-items:stretch;background:var(--bg-primary);
        border-top:1px solid var(--border);
        padding-bottom:env(safe-area-inset-bottom,34px)}
```

CSS 规则是**整条生效或整条不生效**的，不存在 `display:flex` 生效而
`position:fixed` 不生效。而截图里五等分布局、玻璃底、顶边线**全都在**——
按 `index.html:167` 那段注释记的实测值，这条规则要是没生效，五个 button
会塌成 inline-block 挤在 393px 里的左边 0..207，右边一半空着。截图不是那样。

**⇒ 导航栏确实钉在「视口底边」。是视口本身比屏幕矮。**

### 1.2 所以真正的问题是：布局视口在缩，而且每切一次 tab 就缩一截

导航栏是症状不是病灶。页面内容在同一条线上被切断（现象 2）、
下方是纯底色（现象 3），说明**整个 app 的渲染区域只有屏幕的一部分**，
导航栏老老实实钉在这个缩水视口的底边上。

要找的是：**每次 `switchTab()` 之后，`document.documentElement.clientHeight`
为什么会变小，以及为什么不回弹。**

---

## 2. 已经排除的，别再查（附排除方式）

| 方向 | 怎么排除的 |
|---|---|
| 旧缓存 / Service Worker | 用户每次都删主屏图标 → Safari 硬刷 → 重新添加。且 `sw.js` 明确不做离线缓存（只处理 push），`main.py:449` 对 index.html 发 `Cache-Control: no-cache` |
| CSS 语法坏了 / 规则被吃掉 | 12 个 `<style>` 块，`/*` 与 `*/` 各 16 个配平，去注释后 `{` `}` 各 995 个配平 |
| 有别的规则覆盖 position | 全文件所有命中 `#tabBar` 的规则都查过，没有第二条写 `position` |
| 祖先制造 containing block | `#tabBar` 是 `<body>` 直接子元素；`html`/`body` 上没有 `transform`/`filter`/`perspective`/`will-change`/`contain`/`backdrop-filter` |
| `transform` 破坏 fixed 锚定 | `e2b2ccc` 加过 `transform:translateY(var(--tabbar-fix))`，`b289131` 已经删掉，现在没有 |
| 导航栏掉进普通流 | 见 1.1 |

### 2.1 ⚠️ 云端 Chromium 会骗你，八轮里有一半是被它带偏的

**不要拿云端 headless Chromium 的测量结果下任何关于这个 bug 的结论。** 实测差异：

- `env(safe-area-inset-bottom)` 在云端解析成 **0**，导致 `--tabbar-h` 量出来
  **53px**，真机是 **86px**。所有「云端量过了，贴底 0px」的结论，量的都不是
  她那台机器的几何。
- 云端**没有软键盘**，凡是键盘参与的机制在那边 100% 复现不出来。
- Blink 会在元素变 `visibility:hidden` 时自动 blur 掉焦点，**WebKit 不一定**。
  已实测：`exitChat()` 之后 Chromium 里 `activeElement` 变成 BODY。真机上是什么
  **没人验证过**，而这正好是关键路径。

云端能干的只有一件事：验证「我改的东西没把别的弄坏」。**不能用来验证这个 bug 修好了没有。**

---

## 3. 第一步：装探针，先测再修

**在拿到下面这张表之前，不要写任何修复代码。** 八轮全栽在这。

在 `switchTab()` 结尾加一段只记录、不改行为的探针，把每次切 tab 的状态
按行追加进诊断面板（面板已经有了：长按导航栏 1.2 秒弹出，见 `_tbDiag()`）：

```js
window._tbLog = window._tbLog || [];
function tbSnap(tag){
  const bar = document.getElementById('tabBar');
  const r   = bar.getBoundingClientRect();
  const vv  = window.visualViewport;
  window._tbLog.push([
    tag.padEnd(8),
    'vh='   + document.documentElement.clientHeight,
    'ih='   + innerHeight,
    'vv='   + (vv ? Math.round(vv.height) : '-'),
    'scr='  + screen.height,
    'barBot=' + Math.round(r.bottom),
    'gap='  + Math.round(document.documentElement.clientHeight - r.bottom),
    'pos='  + getComputedStyle(bar).position,
    'focus='+ (document.activeElement && (document.activeElement.id || document.activeElement.tagName)),
    'bodyScroll=' + Math.round(document.body.scrollTop)
  ].join(' '));
  if (window._tbLog.length > 40) window._tbLog.shift();
}
```

在 `switchTab()` 最后调 `tbSnap(name)`，并在 `_tbDiag()` 的返回值里把
`window._tbLog.join('\n')` 拼进去。

**要用户做的事**：部署后连点导航栏切 6～8 个 tab，把它窜上去，然后长按导航栏
1.2 秒，截一张诊断面板的图。**这一张图定性，不用再猜。**

### 3.1 怎么读这张表

- **`vh` 每次变小，`gap` 一直是 0** → 布局视口在缩。这是当前最强假设，
  直接进第 4 节。
- **`vh` 不变，`gap` 变大** → 1.1 的推定错了，导航栏真的没钉住，
  回去查 `position` 为什么失效。
- **`vh` 不变、`gap` 是 0，但 `barBot` 变小** → 视口原点被挪了，
  查 `visualViewport.offsetTop`。
- **`bodyScroll` 不是 0** → `body{overflow:hidden}` 仍然可以被 focus 滚动，
  是滚动累积，修法完全不同（见 4.3）。

---

## 4. 三种可能的机制和各自修法（等探针数据出来再选）

### 4.1 主嫌：iOS PWA 键盘把 web view 压小之后没还回来

iOS 主屏 PWA（`apple-mobile-web-app-capable=yes`，`index.html:30`）里，
**软键盘会真的把布局视口压小**，`innerHeight` 跟着变小。这是 iOS 原生行为，
**跟 `interactive-widget` 那个 meta 无关**（WebKit 在 PWA 里不吃它）。
已知的长期 bug：焦点还在某个元素上、而这个元素被弄成不可见（`display:none` /
`visibility:hidden`）时，键盘收起来了但**视口不还原**，留下一片死区。

代码里对得上的地方：

- `body.tab-away #chat{visibility:hidden}`，由 `switchTab()` 挂上。
  `#input` 在 `#chat` 里面。**离开聊天时没有人 blur 它。**
  `8a5b7ed` 加过一个 blur，但只盖了 `#input`。
- `$('pianoSay')` 在 `index.html:6409` 和 `:7487` 会被 focus。切走琴房时
  `#tabMusic` 变 `display:none`，**同样没有人 blur 它**。
- `newChat()`（`index.html:3428` 附近）结尾有 `input.focus()`。

**修法**：在 `switchTab()` 最开头，**换页之前**，无条件收掉焦点：

```js
const ae = document.activeElement;
if (ae && ae !== document.body && typeof ae.blur === 'function') ae.blur();
```

再加一道还原：`focusout` 之后下一帧 `window.scrollTo(0,0)`，逼 iOS 重新量视口。

**验收**：探针表里 `vh` 从头到尾等于 `screen.height`（差值在 safe-area 范围内）。

### 4.2 次嫌：`100dvh` 跟着缩水视口一起塌

`body`、`#chat`、`#tabChats` 都写了 `height:100dvh`（`index.html:127` 等）。
`dvh` 跟的是**动态视口**，视口一缩它就跟着缩，页面自己也矮了一截，
跟现象 2「内容在导航栏那行被切断」吻合。

**这是放大器不是病根**：4.1 修好之后 `dvh` 自然就对了。
**在 4.1 确认之前不要动 `dvh`**，改成 `vh` 会在 Safari 标签页里（有地址栏）
引入新问题。

### 4.3 备选：`body` 被 focus 滚动累积

`body{height:100dvh;overflow:hidden}`（`index.html:127`）。`overflow:hidden`
挡得住手指，**挡不住浏览器因为元素获得焦点而做的 scrollIntoView**。
tab 是 `<button>`，iOS 上点击会给焦点。如果探针里 `bodyScroll` 不是 0 就是它。

**修法**：给 `.tab-item` 的 `pointerdown` 加 `e.preventDefault()` 阻止获得焦点，
或者在 `switchTab()` 结尾把 `document.body.scrollTop` 归零。

---

## 5. 验收标准（三条都过才算修好）

1. 连续点导航栏切 tab **至少 10 次**（五个 tab 来回切），导航栏底边**始终**
   贴着屏幕底，不累积位移。**必须真机验，云端不算数**（见 2.1）。
2. 在聊天里打字弹出键盘 → 收键盘 → 切到别的 tab：导航栏仍然贴底，
   下方没有死区。
3. 页面内容不再在导航栏那一行被切断。具体拿琴房验：进度条底下那排播放控制
   按钮要完整可见。

---

## 6. main 上现在有什么（我加的，随便处置）

`04a602c`。三层，**都没修好这个 bug**，留着不碍事，删掉也行：

| 东西 | 位置 | 建议 |
|---|---|---|
| `interactive-widget=resizes-visual` | `index.html:13` | **留着**。规范默认值，本来也不该写 `resizes-content`。但 WebKit 在 PWA 里不吃它，指望它修好是错的 |
| 离开聊天时 blur `#input` | `switchTab()` 里 | **留着并扩大**，见 4.1。只盖 `#input` 不够 |
| 导航栏常驻不隐藏（删掉 `body.in-chat #tabBar{display:none}`） | — | 用户 03:35 明确要的。**先别动**，等 bug 修好了再问她要不要把自动隐藏加回来 |
| `!important` 焊死块 | `index.html:334` | **可以删**。1.1 已经证明 `position` 没被覆盖，这块是白加的 |
| `weldTabbar()` JS 兜底 | `index.html:7589` 附近 | **可以删**。它拿 `documentElement.clientHeight` 当基准，而那个值本身就是缩水的，量出来 `gap=0` 判定正常，**什么都没做**。这就是用户问「你焊死在哪了」的答案：焊在一块已经缩水的板子上 |
| 诊断面板 + 一行人话结论 | `_tbDiag()` | **留着**，第 3 节的探针直接加在它上面 |

---

## 7. 历史：8 次都改了什么、为什么没成

| commit | 改了什么 | 为什么没成 |
|---|---|---|
| `9d4691a` | 加底部导航栏 + 进 chat 自动隐藏（用户要的功能） | `#tabBar` **一条布局样式都没有**，五个 button 塌在左边、位置跟着内容走。「往上窜」从这里开始 |
| `4086b7e` | `#tabChats` 改成 `height:calc(100dvh - var(--tabbar-h))` | 当时导航栏还在普通流里，这是绕着走，没碰根因 |
| `92f13e7` | 加 `position:fixed;bottom:0;z-index:81` | 方向对，但只加了定位没加布局，塌陷还在 |
| `e2b2ccc` | 补全布局（flex 五等分 / 高度 / 玻璃底）+ 加诊断面板 | 同时加了 `transform:translateY(var(--tabbar-fix))`，把 fixed 元素踢出视口坐标系，导航栏飘到屏幕中间 |
| `b289131` | 删掉那个 `transform` | 定位从这里起就是对的了。**剩下的现象不是定位问题，但后面几轮还在当定位问题修** |
| `4f34249` | tab 页可点击 | 修的是 z-index 相关的另一个问题 |
| `8a5b7ed` | `resizes-visual` + 离开聊天 blur `#input` | 方向可能对，但 WebKit 在 PWA 里不吃 `interactive-widget`，且 blur 只盖了 `#input`，漏了 `pianoSay` |
| `480cefb` | 删掉自动隐藏，导航栏常驻 | 按用户要求改的，不针对本 bug |
| `c625c26` | `!important` 块 + JS 焊死 | 焊在缩水视口的底边上，`gap` 量出来是 0，兜底逻辑判定正常、没有出手 |

**共同教训**：从 `b289131` 之后，每一轮都在改「导航栏怎么定位」，
而真正在变的是**它所定位的那个视口**。加上云端 Chromium 三次给出与真机相反的
结论（2.1），所以越修越远。

---

## 8. 文件索引

单文件：`full-stack/static/index.html`（约 7800 行，CSS 是压缩过的超长行，
`grep -o` 比 `sed -n` 好用）。

| 行 | 内容 |
|---|---|
| 13 | viewport meta |
| 30 | `apple-mobile-web-app-capable`（standalone PWA，键盘会压视口的前提） |
| 127 | `body{height:100dvh;overflow:hidden}` |
| 173 | `#tabBar` 主规则 |
| 334 | `!important` 焊死块 |
| ~3418 | `keyboardOpen()` / `_vhBase` |
| ~3436 | `keepComposerVisible()` |
| ~3751 | `exitChat()` |
| 6409 / 7487 | `$('pianoSay').focus()` ← 4.1 的漏网之鱼 |
| ~7572 | `syncTabbarHeight()` |
| ~7589 | `weldTabbar()` |
| ~7614 | `_tbDiag()` ← 探针加这里 |
| ~7730 | `switchTab()` ← 主战场 |

本地起服务：`cd full-stack/static && python3 -m http.server 8899`，
页面里 `showChat()` 可以跳过密码门。**再说一遍：云端只能用来验证没弄坏别的。**
