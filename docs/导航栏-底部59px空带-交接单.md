# 导航栏底部 59px 空带 —— 交接单

> ## ✅ 2026-09-03 20:51 已修复。真机确认。
>
> 诊断面板：`视口解卡  第1次(vv) 已解卡 61ms  59→0  ✓`、
> `innerHeight 852`、`#tabBar top..bot 762..852`、`离屏幕底 gap 0`。
> 截图逐像素复核：**762..852 整整 90px 是同一个颜色 (250,250,250)**，
> 从导航栏一路到屏幕底，那条 59px 异色带不存在了。
>
> **根因**：iOS PWA 冷启动时布局视口报 793；而小窝自己的
> `body{height:100dvh;overflow:hidden}` 让文档不可滚，视口于是**永远不归位**
> （空白页 300ms 就自愈了，小窝不会）。
> **修法**：`tryUnstick()` 一直待命，条件成立时把外壳松一下再合回去。
> 真机上是 `visualViewport.resize` 那一次触发的，61ms 解决。
> 兜底 B（`body.vp-stuck` + `lvh` 定位）没被用到，留着。
>
> 收尾：导航栏底部留白从整份 `safe-area-inset-bottom`(34px) 收到
> `max(8px, safe-area - 22px)`(12px)，跟 `.composer-wrap` 用同一个公式
> —— 解卡之后她说「下来一些了但还没完全贴底」，量出来图标在 774..807、
> 下面空 45px 上面只有 12px，是导航栏**自己内部**的留白，跟视口无关。
>
> 下面是排查全过程，留作存档。**别再从头猜一遍。**


> **2026-09-02 20:00 结论已定性，见下面第一节。改 CSS 修不好这个 bug。**

## 2026-09-02 23:38 这一轮做了什么（小克）

**那条 59px 空带的颜色定位到了具体哪一行 CSS，已改。**

她量到的带子是 `(243,243,241)`。之前一直没对上任何色号，因为找的方向是
`--bg-primary`(#F8F8F6) 和 `.tab-page` 的渐变末色(#FAF1EE)——都不是。真正的那行在
`index.html` 的手机断点里：

```css
@media(max-width:760px){ body{background:var(--claude-sidebar-bg)} }
                                          /* = #F3F3F0 = (243,243,240) */
```

差 1 个色阶（截图取色的正常噪声）。**死区那块是 canvas 底色，canvas 取的就是 body 的
background**（`html` 自己没有 background，会往上冒泡）。而导航栏是
`--bg-primary` = #F8F8F6，两者差 5 个色阶 —— 这就是「颜色跟页面和导航栏都不一样」。

顺带证实了为什么别的元素盖不住：`#tabBar` / `.tab-page` / `#bgLayer`(壁纸,
`fixed;inset:0`) / `#drawer`(`height:100dvh`) **全部**是 fixed 或 dvh，通通止步 793。
壁纸也够不到那里，所以 Home 页只是「糊过去」，不是真盖住了。

已改成 `body{background:var(--bg-primary)}`（走的是交接单原本的**方向 4**）。
⚠ **这只让死区读起来像导航栏的延伸，没有、也不可能把页面画到 852。**
代价写在代码注释里：抽屉拉开时那条带子不再跟侧栏同色（`#drawer` 用的还是
`--claude-sidebar-bg`）——抽屉是瞬时手势态，导航栏是每个 tab 页常驻的，取后者。

### 🔴 「先确认线上跑的是哪一版」这件事本身是坏的

`BUILD` 常量从 **08-20 起一个字没改过**。09-02 一整天的提交（`d1ca682` 之后那十几个）
全都没动它。所以 19:09 那张诊断表上的 `BUILD 2026-08-20` **既符合旧版也符合新版**，
长按面板根本分不出版本 —— 「在旧版本上调试白耗一轮」的直接原因就是这个。

已 bump 成 `2026-09-02 · 死区底色对齐导航栏`。**从这一版起长按面板才有分辨力。**
以后每次改完记得跟着动（约 8283 行）。

我这边没能自己确认线上版本：浏览器扩展没连上（开不了 Coolify 面板），`~/.ssh` 读不到，
仓库里也没留小窝的域名（`claudepuppy.xyz` 是 Ombre 那台，根路径回的是
claude-memory MCP，`/api/*` 是 nginx 的 401）。**部署后请先长按看 BUILD 那行。**

### `/vptest.html` 补了方向 2 的判据

方向 2 说「先测准 `100dvh` 在塌陷态下等于几」——测试页原来有那个绿框，但**从来没把它的
`getBoundingClientRect()` 打出来**。已补，而且顺手把 `100vh / 100svh / 100lvh` 一起量了
（交接单只想到 dvh，`lvh` 是「浏览器 chrome 收起时的大视口」，塌陷期间有可能仍是 852）。

页面现在直接给结论：
- 哪个单位在塌陷期间仍 = `screen.height` → 直接把那行 `#tabBar{top:calc(100??h - …)}` 印出来
- 四个全跟着塌 → 明说「方向 2 不成立，别再去写 `top:calc(100dvh - …)`」

**一张截图就能定方向 2 的死活。** 打开方式不变：必须从主屏图标进，别先碰屏幕。

### 没动的

方向 1（不用 `fixed;bottom:0`）、方向 3（监听 resize 归位时重新校准）都没动——
在方向 2 的实测数出来之前动它们就是又一轮瞎猜。`#tabBar` / `.tab-page` /
`weldTabbar()` 仍是 `d1ca682` 的原始状态。

---

## 2026-09-03 01:21 第二张 vptest：拍到的是「归位后」，但捡到两条

那张图 `innerHeight 852`、`屏幕高-innerHeight 0`、红条底边 852 —— **视口没塌**，
所以方向 2 和方向 4 都还没答上。（底下 808..852 那 44px 是红条自己，被 `#btns`
的黑罩子压成了 `(46,18,18)`，别当成异色带去取色。已把两根条的 z-index 抬到 3。）

**为什么没塌**：塌陷只发生在冷启动那一下。她那个 vptest 图标多半还在后台活着，
点进去是恢复一个热实例，不是冷启。这一轮的改动就是冲这个去的 —— 页面现在会
**在 t=0 / 第 1 帧 / 300ms / 1200ms 各存一份快照并冻住**，后面就算手滑碰了屏幕，
「启动那一刻」那一栏照样是真值，所有判据也都改成拿快照算，不拿「现在」算。

### 捡到的两条

**1. `100svh` 恒 = 793，`clientHeight` 恒 = 793，而 `vh / lvh / dvh` = 852。**
即使在归位状态下 svh 和 clientHeight 也还是 793 —— 793 = 852 − 59（safe-area-top）。

**2. 🔴 交接单里有一步推理是并错的，得撤回来。**

原文：

> 面板底色 (10,10,12) 到 css 792.7 为止 …… **连全屏覆盖的面板都够不到 793 以下。**
> ⇒ 那块区域页面画不进去

但那个诊断面板是 `position:fixed; inset:0` —— 它**本来就只有 793 高**（ICB 就是
793），到 792.7 为止是它的**尺寸**，不是被裁掉了。「元素只有 793 高」和
「超出 793 就画不出来」是两回事，**后者从头到尾没有被测过**。

这一步很要紧：如果只是尺寸问题，那么任何一个**特意伸到 ICB 外面**的元素都可能
真的画到 852，方向 1/2 就都还活着；如果真是硬裁，那才轮到只能靠 canvas。

### 这一版 vptest 一次问完三件事

| 看什么 | 结论 |
|---|---|
| 屏幕最底下那条是**洋红**还是灰 | 洋红 = 那块归页面管（body 底色盖得到）／灰 = iOS 自己画的 |
| **左边红条**（`fixed;bottom:0`）下沿 | 塌陷期间应停在 793，作对照 |
| **右边青条**（`top:calc(100lvh - 44px)`）下沿 | **到 852 = 页面画得到 793 以下，且 lvh 不跟着塌** |

青条是新加的关键实验：`lvh` 按规范是「可收起的浏览器 UI 全部收起时」的视口高，
而 PWA 里根本没有那种 UI，所以它有可能在塌陷期间仍然 = 852。青条同时把上面
第 2 条那个没测过的问题一起测了 —— 它是特意伸到 ICB 外面去的。

**若青条到了 852 而红条停在 793**，导航栏就是：

```css
#tabBar { top: calc(100lvh - var(--tabbar-space)); bottom: auto; }
```

### 测法（这次必须冷启动）

多任务上划**把 vptest 那一页彻底杀掉** → 点主屏图标进来 → **一下都别碰屏幕** →
直接截图（按电源+音量，不算触摸）。就算不小心碰了，「启动那一刻」那一栏也还在。

---

## 2026-09-03 19:39 冷启动实测 —— 第一节的两条结论要撤回

冻住的四行（iPhone 15 Pro Max，PWA 冷启动，全程没碰屏幕）：

| 时刻 | innerHeight | clientHeight | vh/svh/lvh/dvh | 红条(fixed;bottom:0) | 青条(100lvh-44) |
|---|---|---|---|---|---|
| t=0 | **793** | 793 | 793/793/793/793 | 793 | 793 |
| 第1帧 | **793** | 793 | 793/793/793/793 | 793 | 793 |
| 300ms | **852** | 793 | 852/793/852/852 | 852 | 852 |
| 1200ms | 852 | 793 | 852/793/852/852 | 852 | 852 |

### ❌ 方向 2 作废

塌陷期间 **`vh / svh / lvh / dvh` 四个全是 793**，特意伸到 ICB 外面的青条也只到 793。
视口单位一个都不幸存，`#tabBar{top:calc(100lvh - …)}` 这条不用试了。

（顺带：上一版怀疑的「面板只画到 792.7 是尺寸不是裁剪」这一点仍然没被证实也没被证伪——
青条没能伸出去，是因为 lvh 自己也塌了，不是因为被裁。这个问题先挂着。）

### 🔴 「只有真实触摸能让它归位」是错的

**空白页在 300ms 内自己归位了，她一下都没碰屏幕。**

于是第一节里两条写死的结论一起倒掉：

1. 「⇒ 只有真实触摸能让它归位，代码模拟不了。别再往这个方向投入。」——不是触摸，它自己就好。
2. 「5 个『踢一脚』按钮真机逐个点过，`innerHeight` 一个都没变。」——**那组实验没测到东西。**
   页面 300ms 就归位了，她按按钮至少是几秒之后，`innerHeight` 当时早就是 852，
   「没变」的意思是「一直是 852」。A~E 既没被证实也没被证伪。
   （按钮已从 vptest 删掉，别再拿它当证据。）

### ⇒ 方向变了：是小窝拦住了那次归位

空白页会自己好，**小窝不会**——19:09 那张长按诊断是摸过屏幕之后拍的，`innerHeight`
还是 793。一个自愈一个不自愈，差别就在小窝的代码里，**这件事是找得到的**，
不再是「PWA 实例本身的状态，改样式永远修不好」。

最可疑的一条差别：

- vptest：文档可滚（`scrollHeight 1110 > 852`）
- 小窝：`html,body{height:100%}` + `body{height:100dvh;overflow:hidden}` —— **文档根本不能滚**

### 下一张图要答的（vptest 已改成这个）

页面现在**启动就套上小窝那层外壳**（`height:100%` / `100dvh` / `overflow:hidden`），
第一段冻 t=0 / 第1帧 / 300ms / 800ms / 1400ms；**1500ms 当场把外壳拆掉**让文档可滚，
第二段再冻 4 次。一次launch答两问：

- 第一段一直 793 ⇒ **外壳就是拦路的那个**
- 拆掉之后跳到 852 ⇒ **修法也一起有了**：让小窝启动那一下文档是可滚的
- 第一段照样自己归位 ⇒ `overflow:hidden` 不是原因，换变量再试

而且第一段若真卡在 793，屏幕最底下那 59px 会一直露着——**洋红还是灰，同一张图里
一起答了**。

### 旁证

她今天补充：**iPad 和电脑上都没有这条死区，只有手机有。** 跟 safe-area-top=59
只在刘海机上非零一致，不改变上面的方向。

---

## 2026-09-03 20:37 解卡没生效——被启动时的键盘撞掉了

诊断面板那行直接说了：`视口解卡  跳过（矮了 252px，不是刘海那一档）`。

同一张表里 `切 tab 记录` 的第一行：`chats< … ih=600 … focus=gateInput`。
**登录框在启动时自动聚焦，键盘弹了出来**，那一刻 `screen.height-innerHeight = 252`，
撞上守卫 2「只在塌的量是刘海那一档（1<差<=80）时才动手」，直接跳过。
等键盘收起、视口塌回 793，解卡已经不看了——**它只在启动那一瞬间查了一次**。

判据没错，错在**一次性**。已改成一直待命：`boot` / `resize` /
`visualViewport.resize` / `focusout`（键盘收起，最关键的那一下）/ `pageshow` /
回到前台，任何一次条件成立就动手；成功后自动进入待命，最多试 6 次。

条件不成立时也不再沉默，`window.__unstick` 会写清它在等什么
（「待命（现在矮 252px，键盘之类占着，等它让开）」），面板那行永远说得出话。

🔴 **教训**：iOS PWA 启动时登录框 autofocus 会把键盘带起来，
**任何「在启动那一刻量视口」的逻辑都会量到键盘态**，不是刘海态。

---

## 🎯 2026-09-03 19:54 根因定死，并已修（`index.html` 末尾 `unstickViewport()`）

vptest 套上小窝那层外壳跑的，冻住的数，全程没碰屏幕：

**第一段 · 小窝外壳**（`html,body{height:100%}` + `body{height:100dvh;overflow:hidden}`）

| 时刻 | ih | ch | vh/svh/lvh/dvh | 红(fixed;bottom:0) | 青(100lvh-44) | scrollHeight |
|---|---|---|---|---|---|---|
| t=0 | 793 | 793 | 793/793/793/793 | 793 | 793 | 793 |
| 第1帧 | 793 | 793 | 793/793/793/793 | 793 | 793 | 793 |
| 300ms | 793 | 793 | 852/793/852/793 | 793 | **852** | 793 |
| 800ms | 793 | 793 | 852/793/852/793 | 793 | 852 | 793 |
| 1400ms | **793** | 793 | 852/793/852/793 | **793** | 852 | 793 |

**第二段 · 1500ms 当场放开滚动**

| 时刻 | ih | scrollHeight | 红 |
|---|---|---|---|
| 放开+0帧 | 793 | 1011 | 793 |
| 放开+1帧 | 793 | 1047 | 793 |
| **放开+300** | **852** | 1083 | **852** |
| 放开+1000 | 852 | 1191 | 852 |

### 结论

**文档不能滚的时候 `innerHeight` 永远不归位；一让它可滚，300ms 内自己就回来了。**

⇒ 拦住归位的不是 iOS，是**小窝自己那层 `overflow:hidden` 外壳**。
空白页会自愈，就因为它能滚。第一节那句「是 PWA 实例本身的状态，改样式永远修不好」
到此撤回 —— 它是代码侧的，而且已经修了。

### 修法（已上线）

`index.html` 最末尾的 `unstickViewport()`：启动那一下把外壳松开（`height:auto` +
`overflow:visible` + 一块 1200px 的隐形垫片），等 `innerHeight` 追上 `screen.height`
再原样合回去，全程 ~300ms。

🔴 **垫片必须比视口高出一大截**：小窝的子元素几乎全是 `position:fixed`，不在正常流里，
body 自己的内容高度约等于 0。只给 `min-height:100dvh` 的话 `scrollHeight` 恰好等于
视口高，文档照样不可滚，整段白做。

三层守卫，任何一层不满足就一个字节都不动：
1. 只在 standalone（PWA）里跑 —— 她 09-03 确认 iPad 和电脑上都没这毛病
2. 只在**确实塌了、且塌的量就是刘海那一档**（`1 < 差 <= 80`）时跑；iPad 分屏那种
   天然矮几百的情况不会命中
3. 2.5 秒兜底：无论回没回来都把外壳合上，绝不把 app 永久留在可滚状态

结果记在 `window.__unstick`，**长按导航栏的诊断面板新增一行「视口解卡」** 直接显示
「已解卡 / 超时未解卡 · 用了多少 ms · 塌陷 59→0」，还带一个「合上外壳又塌回去了」的
断言，别再靠猜。

### 备选方案 B（没用上，但记着）

第一段 300ms 之后 `lvh` 自己修正成 852，**青条底边真的到了 852，而那时 `ih` 还卡在 793**。
所以「页面画不到 793 以下」也是错的 —— 元素伸得出去。万一根因修法在某些机型上失效，
`#tabBar{top:calc(100lvh - var(--tabbar-space));bottom:auto}` 是可用的兜底，
但注意冷启动头 300ms 内 `lvh` 也还是 793。

### 验证

长按底部导航栏 1.2 秒 → 看两行：`BUILD` 是不是 `2026-09-03 · 视口解卡（外壳松一下）`、
`视口解卡` 那行写的是什么。再看屏幕最底下还有没有那条 59px。

---

## 真因（已用真机实验确证）

**iOS PWA 启动时 `innerHeight` 少报一个 `safe-area-inset-top`（她机器上 793 而非 852）。
任意一次真实触摸交互会让它归位到 852。塌陷期间 `position:fixed;bottom:0` 钉在 793，
屏幕底部 793..852 成为页面画不到的死区。**

实验：`/vptest.html`（一张不含小窝任何代码的空白页）从主屏图标打开，
同一次会话内前后两张截图：

| | 刚打开 | 用手扒拉一下之后 |
|---|---|---|
| `screen.height` | 852 | 852 |
| **`innerHeight`** | **793** | **852** ✅ |
| `visualViewport.h` | 793 | 852 |
| `scrollHeight` | 793 | 852 |
| `clientHeight` | 793 | 793（**始终不变**）|
| `safe-area top/bot` | 59 / 34 | 59 / 34 |
| 红条(`fixed;bottom:0`)底边 | 793 | 892（页面被拖了 40，fixed 跟着跑）|

空白页也复现 ⇒ **与小窝的 HTML / CSS / Service Worker 完全无关。**

### 这解释了全部历史疑点

- **黑框框 08-20 的真机表是 `innerHeight 852 / vh 793`** —— 那正是「归位后」的形态。
  他测之前必然已经操作过 app，所以前九轮从没见过塌陷态，`bdbffd6` 才会判定
  「布局视口从头到尾没缩过」。那个判定在**归位后**成立，在**刚启动时**不成立。
- 我 09-02 一整天改 CSS 毫无效果 —— 塌的是视口，不是样式。
- 诊断面板报「✓ 视口也是满的」是假阳性：`shrunk=852-793=59`，旧阈值 `>60`，差 1px 漏判。
  （已修：塌陷判定提到几何之前，容差降到 8）

### ⚠️ 纯 JS「踢一脚」全部无效（已实测）

测试页上做了 5 个按钮，真机逐个点过，`innerHeight` **一个都没变**：

| | 手法 | 结果 |
|---|---|---|
| A | `scrollTo(0,1)` → `scrollTo(0,0)` | ✗ |
| B | `body.style.minHeight` 撑高 1px 再收回（强制 reflow） | ✗ |
| C | `documentElement` `display:none` → 复原 | ✗ |
| D | 临时 `<input>` `focus()` 再 `blur()` | ✗ |
| E | `dispatchEvent(new Event('resize'))` | ✗ |

⇒ **只有真实触摸能让它归位，代码模拟不了。** 别再往这个方向投入。

### 因此可走的方向（均未验证）

1. **别用 `fixed; bottom:0` 定位导航栏。** 注意归位后红条底边跑到 892
   （页面被拖 40px，fixed 跟着滚），说明这条路在两种状态下都不稳。
2. 先测准 `100dvh` 在**塌陷态**下等于几（测试页的绿框就是 `100dvh`，
   把它的 `getBoundingClientRect()` 打出来即可）。若它恒等于 852，
   则 `top: calc(100dvh - var(--tabbar-space))` 可用。
3. 或者监听 `resize`，在 `innerHeight` 归位的那一刻重新校准导航栏位置
   —— 但用户不触摸就永远不归位，首屏依旧是坏的。
4. 或者接受 793，用别的手段填掉 793..852（注意：那块区域页面画不进去，
   只能靠 canvas 背景色即 `html`/`body` 的 background 去盖）。

### 复现与验证工具

`/vptest.html`（路由在 `main.py` 的 `vptest()`，文件在 `static/vptest.html`）。
**必须从主屏图标打开**（Safari 里有工具栏几何干扰，测不出来）。
页面会打印全部视口指标并自行判定；5 个「踢一脚」按钮点了会显示
`innerHeight` 前后值，归位则按钮变绿。定性完可连同路由一起删。

---


> 2026-09-02。小朵已经为这个 bug 耗掉一上午，我（web 端）修了 4 次全部失败。
> 这份单子把**量到的事实**和**已证伪的路**写死，接手的人不用再从头猜。

## 症状

所有带导航栏的页面，屏幕最底下有一条 **59px 的空带**，颜色跟页面和导航栏都不一样。
深色纯底的页面（潮汐）最扎眼；壁纸页（Home）能糊过去，但缝一样在。

- 设备：iPhone 15 Pro Max，PWA 独立模式（已添加到主屏）
- 截图 1179×2556 物理 = **CSS 393×852**
- 空带位置：**CSS 793 .. 852**，颜色 `(243,243,241)`

## 已量实的核心事实

**同一台机器、同一个 app 里，两套定位方式给出不同的底边：**

| 元素 | 定位方式 | 底边落在 |
|---|---|---|
| `#chat` / `#tabChats` | `height:100dvh` | **852** ✅ 贴底 |
| `#tabBar` | `position:fixed; bottom:0` | **793** ❌ 差 59 |
| `.tab-page` | `position:fixed; inset:0` | **793** ❌ 差 59 |

### 🔴 这是回归，不是 iOS 固有行为

我一开始推测「iOS 在 black-translucent 下把固定定位包含块做成屏幕高−状态栏高」，
**这个推测是错的**，被 `bdbffd6` 的真机诊断表（08-20 14:11，同一台机器）推翻：

```
#tabBar top..bot   422 .. 852        screen.height 852
vh 全程 793 = 852 - 59（59 是 safe-area-top）
innerHeight 852    visualViewport.h 852    focus 全程 BODY
--tabbar-h         396px   ← 当时的 bug：正反馈把导航栏撑高到 430
```

**`#tabBar` 的底边当时就在 852 = 屏幕底。**`position:fixed; bottom:0` 在这台机器上
**本来到得了 852**。`clientHeight` 是 793 不假（不含刘海），但固定定位包含块是 852。

⇒ 现在它只到 793，是**有东西把固定定位包含块从 852 改成了 793**，要找的是这个。
   拿 `100dvh` 去绕开只是把症状盖住，会把真正的回归留在那儿。

**相关历史**：`d1ca682` 修过一次「布局视口被撑高」——`#modelSheet` 的
`.sheet`(fixed) 被后加载的 `.glass`(relative) 压过，一个 `height:95dvh` 的块留在
正常流里又被 `translateY(100%)` 推下去，`html.scrollHeight` 变成 1619。
修完 scrollHeight 回到 852。**同类机制（某个元素改变了文档/视口的几何）值得优先查。**

### 证据：是视口缩短，不是整页平移

两张聊天页截图对齐量（右上角橙色 `+` 按钮做基准）：

| | 橙色 + 按钮中心 | 输入框特征带 |
|---|---|---|
| 09:15（好） | css **86.8** | 815.3 |
| 14:22（坏） | css **86.7** | 756.3 |
| 差 | **0.1px** 顶部钉死 | **59.0px** |

## 🔴 09-02 19:09 真机诊断表 —— 真因已定性：布局视口塌了

小朵长按导航栏拍的表（同机 iPhone 15 Pro Max）：

```
BUILD  2026-08-20 · 头像裁切收尾     standalone  true
innerHeight          793      ← 08-20 那次是 852
docEl.clientHeight   793
visualViewport.h     793
screen.height        852      dpr 3
safe-area  top/bot   59 / 34  ← 非 0，说明 viewport-fit=cover **是生效的**
屏幕高-布局视口高      59
当前焦点             BODY     ← 不是键盘吃的
#tabBar  position fixed   top..bot 703..793   高 90   --tabbar-h 56px
祖先链  NAV#tabBar / BODY pos=static / HTML pos=static   ← 无 transform 祖先
```

**布局视口 = 793，而且画在屏幕的 0..793**。底部 793..852 是页面**根本画不到**的死区。

旁证：诊断面板自己是 `position:fixed; inset:0`，量它的截图——
面板底色 `(10,10,12)` 到 **css 792.7** 为止，793..851.7 是 `(25,25,25)`。
**连全屏覆盖的面板都够不到 793 以下。**

⇒ **这不是 CSS 问题。** `#tabBar` / `.tab-page` / `--tabbar-h` 全部健康
（高 90、position fixed、无 inline、无 transform 祖先）。改任何 CSS 都盖不住那 59px，
因为那块区域页面画不进去。我 09-02 上午改一天 CSS 毫无效果，原因就在这。

### 面板当时报了绿灯，是假阳性（已在同一提交里修掉）

```js
const shrunk = screen.height - innerHeight;   // 852 - 793 = 59
else if (shrunk > 60)  v = '△ 布局视口比屏幕矮…'   // 59 不满足，漏过去
else                   v = '✓ 导航栏钉在底部，视口也是满的'
```

阈值 60 是当初为躲开「刘海 59 的坐标系假警报」设的，这次真塌了 59，**差 1px 漏判**。
`gap=0` 同样是假的——它拿 `innerHeight` 当基准，而 `innerHeight` 自己塌了，
gap=0 只说明「贴到了布局视口的底」。
现已改成：视口塌陷**排在导航栏几何之前**判，容差降到 8px。

### 下一步该查什么

`viewport-fit=cover` 生效、meta 从 08-20 至今一字未改、无 theme-color / 无 manifest、
焦点是 BODY、祖先链无 transform —— 代码侧的常见嫌疑**全部排除**。
`innerHeight` 从 852 变成 793 是**环境状态**，优先按这个顺序试（都不用改代码）：

1. **完全杀掉 app**（多任务上划掉）再打开 —— 排掉「启动/后台恢复时视口协商卡在错误尺寸」
2. **删掉主屏图标，重新「添加到主屏」** —— iOS 在安装时固化 PWA 显示配置，重装会重新协商
3. 若 1、2 都无效：确认手机 iOS 版本在 08-20 之后是否更新过（新版可能改了 PWA 视口行为）

**只有以上都排除，才轮到改代码。** 那时再考虑用 `100dvh` 兜底（见下文），
但要清楚那只是把死区让开，不是让页面画到 852。

## 已知好的版本

**`d1ca682`**（09-02 00:38 UTC = 08:38 北京）。
09:15 北京的真机截图证实：聊天页输入框贴底 852，底部只有 3.3px 的内容边缘，**无缝**。
（注意：聊天页看不到导航栏——`body.in-chat #tabBar{display:none}`。所以这张图只证明
`#chat` 贴底，**没有**证明导航栏当时贴底。导航栏的缝在 `d1ca682` 可能就存在。）

## ⚠️ 第一步必须先做

**确认线上跑的是哪个版本。**
**仓库当前状态（重要）：** 导航栏相关的代码 —— `#tabBar` / `.tab-page` /
`weldTabbar()` / head 里那块注释 —— 全是 `d1ca682` 的原始状态，我 4 次乱改一处
都没留（`19e5765` 全量还原过一次，之后只把潮汐页和「忱的心绪」拿了回来，
**没碰导航栏**）。所以直接在 main 上改就行，不用先回滚。

14:37 的截图数值（792.3 / 59.7px / `(243,243,241)`）跟 14:22 **完全一致**，
高度怀疑当时还原没部署上去。先确认线上版本，别在旧版本上调试。

app 里有真机诊断：**长按底部导航栏 1.2 秒**弹出，`BUILD` 那一行是版本。

## 已证伪的路（别再走）

| # | 提交 | 做了什么 | 为什么失败 |
|---|---|---|---|
| 1 | `d851579` | 复原 `c625c26` 焊死块，`weldTabbar()` 改用 `innerHeight` 基准 | 用户驳回。焊死块本身在 08-20 就被判定无效 |
| 2 | `0300fa8` | 给 `#tabTidal` 加负 `bottom`，把页面背景往下延伸盖缝 | **缝在导航栏下面**，染色改变不了几何，肉眼几乎无差别 |
| 3 | `ac6d7d0` | `@media (display-mode:standalone)` 里给 `#tabBar`/`.tab-page` 补偿 | **引入新 bug**：里面写了 `#chat{height:auto}`，把 `#chat` 唯一有效的 `height:100dvh` 干掉了，**聊天页也开始有缝**。已随 `561f074` 撤回 |
| 4 | `561f074`+`5453a60` | 全量还原 `static/` 到 `d1ca682` | 待验证部署 |

**另外两条历史坑（更早的会话踩过）：**
- 拿 `documentElement.clientHeight` 当基准算「离屏底几 px」——它恒等于 793，算出来永远是 0，
  焊死逻辑一次都不会动手。这是 08-20 误判「位置从来没错过」的根源。
- 给 `#tabBar` 加 `transform`（`e2b2ccc` 的 `--tabbar-fix`）——会把 fixed 元素踢出视口
  坐标系，导航栏飘到屏幕中间。`translateX(--drawer-x)` 是侧栏联动，**必须保留**，别写 `transform:none`。

## 修法

### 第一优先：先找回归（因为 fixed 本来能到 852）

按 `d1ca682` 的路子查「谁改变了视口几何」：

```js
// 真机 / Safari 远程调试里跑
document.scrollingElement.scrollHeight   // 应 == clientHeight，被撑高就是有元素跑出流
innerHeight, document.documentElement.clientHeight, visualViewport.height
$('tabBar').getBoundingClientRect()      // 底边应为 852
```

重点怀疑：留在正常流里、又被 transform 推走的大块元素（`.sheet` / `.fsheet` /
`.letter-overlay` / `#modelSheet` 这类），以及**双写陷阱**——同一个选择器在
`index.html` 内联 `<style>` 和 `design-system.css` 里各写一份，后加载的赢。
黑框框栽过两次（`bdbffd6`、`d1ca682`），两次都是「全文件查过」只查了 index.html。

### 兜底（仅在确认不是回归、就是 iOS 行为变化时才用）

```css
#tabBar {
  top: calc(100dvh - var(--tabbar-space));
  bottom: auto;
}
.tab-page {
  height: 100dvh;
  bottom: auto;   /* 让 top:0 + height 说了算，跟 #chat/#tabChats 一致 */
}
```

几何验算（`--tabbar-space` = 56 + 34 = 90）：
- `top = 852 − 90 = 762` → 导航栏 **762..852**，贴底，高度不变，图标不跑位
- `.tab-page` 0..852，其 `padding-bottom:90px` 内容止于 **762** = 导航栏顶边，严丝合缝

**风险点，必须真机确认：**
1. `100dvh` 在键盘弹起时会不会变（viewport meta 是 `interactive-widget=resizes-visual`，理论上不变）
2. 转屏 / 分屏下 `100dvh` 是否仍等于视口高
3. `body.keyboard-open #tabBar` 那条 translate 收起逻辑不受影响（改的是 `top`，不是 `transform`）

## 真机诊断面板（黑框框留的，优先用它）

**长按底部导航栏 1.2 秒** → 震动一下 → 黑底诊断表 → 截图 → 点任意处关闭。

它直接给出：`BUILD`（**线上版本，先看这行**）、`standalone`、`innerHeight`、
`docEl.clientHeight`、`visualViewport.h`、`screen.height`、`safe-area top/bot`、
`屏幕高-布局视口高`、`#tabBar` 的 position/display 和实际 top..bot、切 tab 记录。

代码在 `index.html` 的 `_tbDiag()` / `openTabbarDiag()` / `bindTabbarDiag()`。
**改完之后记得把 `BUILD` 常量更新**（约 8263 行），否则分不清手机上是不是新版本。

## 怎么验证（截图量法，比肉眼靠谱）

改完让小朵截图，跑这个：

```python
from PIL import Image
im = Image.open('截图.png').convert('RGB')
w, h = im.size
print('CSS', w/3, h/3)          # 应为 393 x 852
x, prev, top = 20, im.getpixel((20, h-1)), h-1
for y in range(h-2, h-260, -1):  # 左边缘往上扫，避开卡片内容
    c = im.getpixel((x, y))
    if max(abs(a-b) for a, b in zip(c, prev)) > 3:
        print(f'底部色带 css {(y+1)/3:.1f}..851.7  高 {(top-y)/3:.1f}px  {prev}')
        break
```

- **修好了**：底部色带高度 < 5px（只有内容边缘）
- **没修好**：底部色带 ≈ 59px，颜色 `(243,243,241)`

需要逐像素对齐两张图时，用右上角橙色 `+` 按钮当基准（聊天页）：
顶部对齐说明是视口缩短，顶部也偏移说明是整页平移。

## 相关文件

- `full-stack/static/design-system.css` — `#tabBar`（约 332 行）、`.tab-page`（约 424 行）、`--tabbar-space`（约 324 行）
- `full-stack/static/index.html` — `weldTabbar()` / `syncTabbarHeight()`（约 8200 行）、viewport meta（约 30 行）、`_tbDiag()` 真机诊断面板
- viewport meta 现状：`width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover,interactive-widget=resizes-visual`
- PWA meta：`apple-mobile-web-app-capable=yes` + `apple-mobile-web-app-status-bar-style=black-translucent`，
  **head 里不能出现 `theme-color` 或 `link rel=manifest`**，否则状态栏退回实心（注释在 index.html 第 14–41 行）
